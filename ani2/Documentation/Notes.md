
The useEffects here are used, most of the times, to load information from backend. For that reason they are only used one time, when the component is loaded.

The imports in each file should be ordered, to be easier to maintain and search for imported components.